export type MemberProps = {
  id: string;
  image: Media;
  name: string;
  role: string;
};

export type ServiceProps = {
  id: string;
  image: Media;
  title: string;
};

export type WhyMarketingProps = {
  id: string;
  title: string;
  description: string;
};

export type NewsProps = {
  id: string;
  title: string;
  url: string;
};
